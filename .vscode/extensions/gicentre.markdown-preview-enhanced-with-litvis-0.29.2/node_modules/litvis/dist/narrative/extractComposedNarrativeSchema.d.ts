import { VFile } from "vfile";
import { LitvisNarrative } from "../types";
export declare const extractComposedNarrativeSchema: (narrative: LitvisNarrative, filesInMemory?: VFile[]) => Promise<void>;
