import unified from "unified";
import { VFile } from "vfile";
import { LitvisDocument } from "../types";
export declare const engine: unified.Processor<unified.Settings>;
export declare const parseDocument: (vFile: VFile) => Promise<LitvisDocument>;
