import { Position } from "unist";
import { LitvisDocument } from "../../types";
export declare const extractElmSourceDirectories: (dataWithPosition: any, document: LitvisDocument) => {
    paths: string[];
    positions: Position[];
};
