import { DataWithPosition } from "data-with-position";
import { LitvisDocument } from "../../types";
export declare const extractNarrativeSchemas: (dataWithPosition: any, document: LitvisDocument) => {
    pathsWithPosition?: DataWithPosition;
};
