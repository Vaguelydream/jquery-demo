import { Position } from "unist";
import { LitvisDocument } from "../../types";
export declare const extractFollows: (dataWithPosition: any, document: LitvisDocument) => {
    value?: string;
    position?: Position;
};
