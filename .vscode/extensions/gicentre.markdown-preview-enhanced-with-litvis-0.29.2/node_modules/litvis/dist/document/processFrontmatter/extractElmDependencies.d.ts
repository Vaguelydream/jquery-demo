import { Position } from "unist";
import { LitvisDocument } from "../../types";
export declare const extractElmDependencies: (dataWithPosition: any, document: LitvisDocument) => {
    versions: {
        [packageName: string]: string | false;
    };
    positions: {
        [packageName: string]: Position;
    };
};
