import { ComposedNarrativeSchema } from "narrative-schema-common";
export declare const applySchemaToLabels: (composedNarrativeSchema: ComposedNarrativeSchema) => () => (ast: any, vFile: any, next: any) => any;
