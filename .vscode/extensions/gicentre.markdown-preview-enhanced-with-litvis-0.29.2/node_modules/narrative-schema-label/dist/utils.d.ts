import { VFile } from "vfile";
import { LabelErrorType, LabelNode, LabelType } from "./types";
export declare const getLabelIdPrefix: (vFile: VFile) => string;
export declare const deriveLabelType: (start: string, end: string) => LabelType;
export declare const markLabelNodeAsErroneous: (vFile: VFile, labelNode: LabelNode, errorType: LabelErrorType, errorCaption: string) => void;
export declare const isValidLabelName: (name: string) => boolean;
