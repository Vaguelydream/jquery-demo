import { DataWithPosition } from "data-with-position";
import { NarrativeSchema, ParentDocument } from "narrative-schema-common";
import { VFile } from "vfile";
export declare const load: (dependenciesWithPosition: DataWithPosition, parents: Array<ParentDocument | NarrativeSchema>, filesInMemory: VFile[], schemasAlreadyLoaded: NarrativeSchema[]) => Promise<NarrativeSchema[]>;
