import { DataWithPosition } from "data-with-position";
import { EntityDefinition, NarrativeSchema } from "narrative-schema-common";
export declare const extractDefinitions: (dataWithPosition: DataWithPosition, narrativeSchema: NarrativeSchema) => EntityDefinition[];
